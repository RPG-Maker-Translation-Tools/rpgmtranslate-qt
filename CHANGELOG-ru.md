# Список изменений

Все крупные изменения в проекте документируются в этом файле.

Формат базируется на [Keep a Changelog v2.0.0](https://keepachangelog.com/en/2.0.0/).
Проект использует (или хотя бы пытается использовать) [семантическое версионирование](https://semver.org/spec/v2.0.0.html).

Даты в формате DD.MM.YYYY.
Версии могут иметь кодовое имя рядом с версией.

## [Нерелизнуто]

## [v1.1.1] "Смерть RPG Maker форумов" - 04.09.2026

### Добавлена

- Версия библиотеки `mimalloc` теперь показывается в окне "О программе".
- Добавлена кнопка базы данных. Она выключена и ничего не делает. Она будет сделана, когда будет сделана база данных.

### Изменено

- Опубликована обновлённая документация.
- Файл `js/plugins.js` теперь копируется в качестве базы рядом с обычными data файлами.
- Статус-меню теперь напрямую говорит что результаты записи могут быть найдены в `.rpgmtranslate/output`.
- Записи в меню ассетов теперь сортируются по названию файла.
- `baseline-data` теперь НЕ просто папка для data файлов рпг мейкера. Теперь он содержит как директорию `data`, так и директорию `js` (только MV/MZ, хранит `plugins.js`). Обратная совместимость сохранена.

### Фиксы

- Для игр MV/MZ, починили ошибку про файл `js/plugins.js` при записи.
- Починили путь для копирования обновлённых файлов в папку базы, которая не учитывала RPG Maker 2000/2003.
- Исправлено использование файлов из базы для повторных чтений из меню rvpacker, что приводило к отсутствующему эффекту от чтения.

### Разработка

- Исправлено недобавление релизных артефактов в релизы на GitHub.
- Добавлен файл `.gitattributes` чтобы всё было в LF.

### Запланировано на 1.2

- Отображение информации о словах/символах в исходном тексте/переводе.
- Отображение имени записей в которых текст был найден, при, например, чтении.
- Проверить, какие плагины нуждаются в завершении и улучшении в плане линтинга, и проверить всё ли совместимо с VisuStella.
- Встроенная база данных как в RPG Maker для поиска отдельных записей и быстрого поиска.
- Проигрывание WAV для RM2K.
- Просмотр файлов сохранений.

### Запланированно на 1.3

- Поддержка удалённых точек для встроенного git-клиента.
- Поддержка Wolf RPG.
- Поддержка VisuStella (если необходимо).

## [v1.1.0] "Смерть RPG Maker форумов" - 01.09.2026

Большинство работы для этого релиза было сделано вне `rpgmtranslate-qt`.

### Добавлено

- Поддержка RPG Maker 2000/2003.
- Так как RPG Maker 2000/2003/XP/VX не поддерживают юникод, следующее было добавлено:
  - Добавлен выбор кодировки текста при чтении.
  - Добавлен выбор кодировки для записи в настройках.
- Перевод на упрощённый китайский [#17](https://github.com/RPG-Maker-Translation-Tools/rpgmtranslate-qt/pull/17), спасибо @grill-glitch

### Изменено

- Обновлено до `rvpacker-txt-rs-lib` v16.2.0:
  - **Поддержка RPG Maker 2000/2003.** Определяется по файлам `.ldb`/`.lmt`/`.lmu`.
  - **Кодировка текста теперь отдельная необходимая опция.** Это нужно для RM2K, XP и VX, которые не содержат никакого индикатора кодировки (ранее мы просто угадывали кокдировку из списка UTF-8/Windows-1252/Windows-1251/Shift-JIS/GB18030). VX Ace на практике не нуждается в этом: его формат маркирует строки их кодировкой, и вместо угадывания эта кодировка и используется.
  - Улучшена производительность.
- Стандартная горячая главиша глоссария изменена на `Alt+L`.
- Из настроек удалён выборщик языка, так как он уже доступен из полоски меню.

### Фиксы

- Исправлен бредовый текст в окне "О программе".
- Исправлена неверная версия в окне "О программе".
- Одна и та же горячая клавиша теперь не может использоваться в нескольких местах, настройки будут отклонять такой расклад.
- Файл "Areas" движка VX теперь не учитывается в проверке исходных файлов против базы и не вызовет всплывающее окно "Исходные файлы были обновлены".
- Исправлена ошибка обновления 301 на Windows, больше информации в секции разработки.
- Исправлено отсутствие иконки у кнопки для добавления новой колонки в таблице перевода.
- Mimalloc теперь правильно используется вместо других аллокаторов.

### Разработка

- Сторона Rust теперь использует аллокатор, предоставляемый из C++, так что вся логика аллокации объединена и если присутствует аллокатор, который заменяет стандартный, Rust использует его без изменений.
- Обновили CI чтобы автоматически добавлять билд артефакты в опубликованные релизы.
- Исправлена русская документация.
- Qt теперь компилируется с настройками `-ssl -schannel` и не проваливается при попытке перехода по `https` ссылкам, что приводило к ошибкам 301.
- Qt обновлён до 6.11.2.
- Все расширения путей в CMake скрипте теперь находятся в кавычках, что исправит любые проблемы с путями содержащими пробелы.
- Добавлена CMake опция `LUA_EXECUTABLE` чтобы использовать системный Lua вместо распространяемого в проекте.
- Добавлена CMake опция `RUST_PROFILE` для указания профиля Rust для билда Rust стороны.
- Билды на Windows теперь компилируются с LLVM 23.

## [v1.0.1] "Смерть RPG Maker форумов" - 24.08.2026

### Изменено

- Обновлено до `rvpacker-txt-rs-lib` v15, ниже находятся переведённые заметки о релизе.

**Маркер комментариев теперь `<!>`**

Раньше строки с метаданными и комментарии использовали форму `<!-- ... -->`. Закрывающий маркет почти никогда не проверялся, и разметка плагинов RPG Maker почти никогда не использует такие короткие теги, так что открывающий маркер теперь обозначает комментарии. После маркера не стоит пробел, наименование начинается сразу после.

```text
<!--  ID  --><#>1              →  <!>ID<#>1
<!-- NAME --><#>Potion         →  <!>NAME<#>Potion
<!-- Ignore Entry --><#>Items: 1  →  <!>Ignore Entry<#>Items: 1
```

**Обработка отдельных игр уничтожена**

`GameType` и каждый параметр `game_type` были удалены. Они были заменены двумя техниками:

- **Разметка плагинов теперь обрабатывается отдельно.** Note тэги плагинов Yanfly `<Menu Category: ...>` или Advanced Text System из VX Ace - префиксы `\et[n]`, `\nbt` и `\nblt` обрабатывались только при обработке конкретной игры. Теперь они поддерживаются независимо от игры, основываясь на движке.
- **Всё что не нужно должно быть в `.rvpacker-ignore`.** Строки которые не нужны в переводе отправляются в этот файл, который теперь понимает глоб паттерны:

  ```text
  <!>Ignore Entry<#>Items: 1
  Torch
  <!>Glob<#>Soul of*
  ```

  `*` совпадает с любым количеством символов, `?` только с одним; всё остальное совпадает как есть.

**Удалили `Trim` из `BaseFlags`**

Обрезка пробелом из исходного текста сначала выглядела как что-то удобное. Она позволяла сократить одинаковые исходные строки, но с разными пробелами в одну строку. Но это кал вонючий.

Для начала, если разработчик поместил несколько линий, логически означающих одно и то же, но с разными пробелами (к примеру у одной строки нет конечного пробела, у другой есть) - это их проблема. Мы не может просто подчистить всё в один вариант, потому что эти строки - большой объект для последующих изменений. И мы не будем исправлять любые текстовые баги вместо разработчиков, просто чтобы сделать что-то "удобно" или "точно". Строки с разными пробелами должны чинится разрабами, а не этим инструментом.

Вторая вещь - `Trim` просто был слишком разрушительным. Много RPG Maker игр полагаются на трюки с проблеами, например чтобы создать индентацию для текста (или центрировать его). Поскольку просто гигантское количество RPG Maker игр это делает, `Trim` почти всегда был неподходящей опцией. И поэтому был удалён.

**Потеря данных**

- **Очищенные записи никогда не использовались при чтении.** Очистка собирает свои игнорируемые записи в конце обработки, и получалось так что записям присваивался айди который был обработан последним - так что каждая очищенная линия попадала в финальную секцию `.rvpacker-ignore` и не использовалась. Файл был по сути бесполезен.
- **Чтение с добавлением добавляло строки без исходного текста в переведённые файлы.** Заголовки ивентов используются в обработке как записи с пустым исходным текстом; код который их записывал ничего не проверял, так что для некоторых секций ивентов линия теряла исходный текст и перезаписывалась переводом.
- **Чтение с добавлением для map файлов и опцией `map_events` теряло весь перевод после первого события.** Карты собираются раз за ивент, и только первый из собранных блоков имел перевод.
- **Поля с конечными пробелами не могли быть записаны обратно.** Запись обрезала каждую исходную строку перед её проверкой, а чтение лишь обрезает сообщения, из-за чего ключи не совпадали. The write path trimmed every line of a source before looking it up; the read path only trims messages, or everything under `BaseFlags::Trim`. The keys never matched. VX Ace's default actor biographies are affected.
- **Строки плагинов с переносами строк не могли быть записаны обратно.** Запись плагинов искала перевод по нормализованной форме `\#`, в то время как распарсенные ключи были денормализованы `\n`.
- **Игровая валюта никогда не парсилась для MV/MZ.** `Labels::currency_unit` был неизменной строкой `currency_unit`, но в MV и MZ игровая валюта - `currencyUnit`; так что обработка пропускала эти движки.
- **Название игры при чтении не использовалось.** `set_game_title` использовал название только в режиме чтения, а обработка при записи никогда его не использовала.
- **`.rvpacker-ignore` проверял только первую секцию.** Первая секция в файле имела маркер в ключе, а каждая послудеющая теряя его. Секция без суффикса `: id` также производила чтение после конца строки.
- **`Mode::Write` и перезаписывающее добавление превращались в тот же байт при сериализации**, так что перезаписывающее добавление считалось за запись.

**Другое**

- Отсутствующие файлы перевода (вроде отсутствующего states.txt) теперь не будут возвращать ошибку, а просто будут выводить предупреждение.
- Пустые записи в `.rvpacker-ignore` теперь не записываются в сам файл, что сильно снижает его размер.

**Миграция**

1. Конвертируйте существующие `.txt` файлы: `<!--` → `<!>` и удалите `-->` на всех комментариях.
2. `BaseFlags::Trim` удалён - вам нужно поправить свои исходники в необрезанные их версии. Самый лёгкий путь - создать копию старого перевода, перечитать игровые файлы с нуля, затем ручками всё портировать.

### Разработка

- Починили невозможность скомпилировать приложение с некоторыми отключёнными опциями, потому что код был не защищён от этого.
- Добавили больше опции в CMakeLists чтобы билды были модульными.
- Linux билды в докере теперь скомпилированы с архитектурой `x86-64-v3`.
- Поправлена документация, в особенности русская.
- Стандартные Linux/AppImage билды теперь предоставляются через GitHub Actions CI.

## [v1.0.0] "Смерть RPG Maker форумов" - 21.08.2026

### Добавлено

- Поддержка и линтинг плагинов Yanfly и подобных. Информация: <https://rpg-maker-translation-tools.github.io/rpgmtranslate-qt/ru/text-editing>.
- Реализованы всплывающие подсказки линтов при наведении. Подсказки содержат полезную информацию о тегах.
- Несколько новых линтов, таких как несовпадение тегов, переключаемая проверка орфографии/глоссария.
- Реализована специальная раскраска для тегов вроде \C[x] или \hc[rrggbb].
- Реализованы локальные функции для Git клиента, удалённое взаимодействие ещё не реализовано.
- Реализована поддержка LanguageTool. Платный API LanguageTool не протестирован, только локальный сервер.
- Окно "О программе" теперь показывает информацию об окружении сборки (компилятор, STL, SDK, дата), а также версии ключевых встроенных библиотек (например Nuspell).
- Запущенную задачу (например поиск) теперь можно прервать кнопкой рядом с текстом задачи.
- Приложение теперь предупреждает вас, если файл перевода содержит строки, которые не удалось распарсить, вместо того чтобы молча их пропускать.
- Экспорт/импорт: файлы переводов теперь можно конвертировать в и из CSV, XLSX, XML, JSON и YAML новым пунктом "Экспорт/импорт" в меню кнопки `rv`.
- Кнопка "Отформатировать" просмотрщика ассетов теперь также форматирует JS и Ruby скрипты (через `dprint-plugin-typescript` и `librubyfmt`), а не только JSON.

### Изменено

- Обновлено до `rvpacker-txt-rs-lib` v14.0.0:
  - Файлы теперь сортируются перед чтением, это означает, что они всегда будут в правильном порядке (например actors первый, weapons последний, map001-map999 последовательно).
  - Удалена функция romanize так как она не имела реального значения - "романизация" (или более корректно, латинизация) текста должна выполняться пользователями библиотеки и должна влиять только на перевод - не на исходный текст.
  - Реализован модуль `serde` для экспорта текста в разные структурированные форматы и импорта их обратно. Такие форматы включают CSV, XLSX, YAML, JSON, XML.
- Цвет линтов теперь золотистый по умолчанию.
- Активные линты, их внешний вид и поведение настраиваются через параметры.
- Замены текста на лету (например преобразование `<<` в `«`) теперь настраиваются через параметры.
- Меню массовой обработки теперь работает с кодом JavaScript для выполнения массовых действий. Для реализации использовался quickjs-ng. Он стремится поддерживать новейшие спецификации ECMAScript чтобы вы не были ограничены при написании скриптов.
  - Вы можете создавать свои собственные скрипты! Документацию по тому как это работает в rpgmtranslate-qt можно найти здесь: <https://rpg-maker-translation-tools.github.io/rpgmtranslate-qt/ru/batch-processing/>.
- Микрооптимизированы разные части программы.
- Отслеживание текущей задачи в верхней панели теперь более информативно.
- Меню массовой обработки теперь имеет два выбираемых действия, "перевод" и "скрипт". "скрипт" имеет три встроенных скрипта, включая обрезку и перенос из прошлого, и новый скрипт латинизации.
- Селекторы исходного/целевого языка переработаны для отображения полного спектра человеческих языков. Ранее эти поля включали только языки поддерживаемые бэкендом Snowball. Если язык не поддерживается [Snowball](https://snowballstem.org/algorithms/) (или LSTM сегментацией), он не будет поддерживать стемминг и уведомит вас об этом.
- Меню совпадения терминов переработано как общее меню для линтов. Таблица упрощена чтобы показывать общие линты о несовпадениях тегов, несовпадениях пунктуации, опечатках, несовпадениях терминов и всём остальном.
- Активные линты и другие проверки переводов теперь переключаются в новом меню линтов. Это позволяет вам переключать линты сразу же, однако линты всё ещё настраиваются в параметрах.
- Логирование значительно улучшено, теперь оно должно охватывать больше частей приложения и быть лучше в целом.
- Каждая задача теперь запускается в собственном выделенном потоке, что позволяет вам запускать несколько задач одновременно.
- API ключи (точки перевода, LanguageTool) больше не хранятся в `settings.json`. Они теперь находятся в хранилище учетных данных ОС (Windows Credential Manager / Linux Secret Service) и читаются в память только при необходимости.

### Фиксы

- Git клиент/панель вкладок теперь скрываются перед открытием проекта.
- Git клиент/панель вкладок теперь скрываются и Git клиент правильно сбрасывается при закрытии проекта.
- Исправлены проблемы возможно связанные с хешированием из-за хеширования мусора после нулевого терминатора.
- Исправлена загрузка неправильных хешей из параметров проекта, что вызвало бы всплывающее окно с сообщением что файлы обновлены, когда на самом деле они не обновлялись.
- Исправлено отображение меню на неправильной Y позиции на Windows.
- Исправлены неправильные значения столбца перевода / точки перевода в меню массовой обработки.
- Исправлены сломанные меню выбора файла, которые были сломаны по какой-то причине. Или может это регрессия которая никогда не была отправлена в кандидатах релиза.
- Таблица переводов теперь прокручивается горизонтально пиксель за пикселем что удобнее.
- Действие "запись" на панели инструментов больше не показывает ложное сообщение "успешно записано" после того как запись на самом деле не удалась.
- Окно параметров больше не записывает устаревший индекс точки перевода после того как эта точка была только что удалена.
- Метка процента масштабирования в просмотрщике ассетов теперь правильно показывает знак `%`.
- Масштабирование по размеру в просмотрщике ассетов (при открытии изображения или обновлении примера шрифта) теперь работает корректно.
- Исправлена ошибка копирования где точка перевода Mistral случайно использовала API Moonshot для перевода одной ячейки.

### Разработка

- Скрипты конфигурации перенесены на Lua и начато распространение минимальных билдов Lua для Windows/Linux для запуска скриптов конфигурации. Модифицированные источники Lua можно найти здесь: <https://github.com/savannstm/lua>.
- Парсинг/запись JSON теперь использует Glaze, что более поддерживаемо.
- Пересмотрены флаги используемые для билда проекта на Windows - теперь должно быть более оптимизировано и меньше ошибок. Я также обрезал встроенный билд данных ICU, так что сырой исполняемый файл теперь занимает меньше места.
  - Что-то повредило бинарники после сжатия их через UPX, который мы использовали для каждого релиза и приложение крашилось при запуске - #12. Проблема была в сжатых экспортах. После переупаковки с `--compress-exports=0`, проблема исчезла, так что теперь наши сжатые распространяемые бинарники **менее 20МБ**!
- Теперь мы поставляем лицензии для всех библиотек которые мы статически линкуем. Лицензии можно найти в директории `licenses`, и окно "О программе" теперь содержит информацию о таких библиотеках. Это наше юридическое обязательство, но я также решил включить библиотеки которые не требуют указания авторства.
- Добавлен скрипт `prepare-release.lua` для упаковки релиза который проверяет что каждая встроенная библиотека имеет задокументированный текст лицензии, генерирует уведомление о библиотеках третьейстороны, сжимает построенный бинарник через UPX и пакует итоговый архив релиза.
- Документация (параметры, git, массовая обработка, глоссарий, редактирование текста) была обновлена чтобы отражать текущее состояние программы и её возможностей.
- Официальные релизы теперь нацелены на архитектуру x86-64-v3 (вместо предыдущих билдов на архитектуру Sandy Bridge) - начиная с Intel Haswell (2013) и AMD Excavator (2015). Это позволяет использовать AVX2, FMA и BMI чтобы значительно улучшить производительность. Если вам нужна сборка поддерживающая более старые процессоры, вам придется собирать из исходников.
- JS скрипты приложения по умолчанию теперь можно найти в директории `scripts`.
- Заменен ручной алгоритм подсветки через tree-sitter (аллокация буферов, инкрементальный `QSyntaxHighlighter`, закрепление цвета за токеном) на [lumis](https://github.com/leandrocp/lumis): Rust теперь сразу же передает HTML, и просмотрщик кода просто его загружает.
- Исправлена давняя опечатка в опции CMake для подсветки синтаксиса JS что означало что она никогда не была включена, независимо от значения.
- Релизы для Windows теперь используют `mimalloc` в качестве стандартного аллокатора.

### Запланировано

- Завершение всех возможностей запланировано на следующие два релиза: 1.1 и 1.2.
- Поддержка удалённых точек для встроенного Git клиента.
- Линтинг большего количества поддерживаемых плагинов (включая VisuStella когда-нибудь)!
- Встроенная база данных как в RPG Maker для поиска конкретных записей и быстрого поиска чего угодно через поиск.
